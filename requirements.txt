# CareVoice — Gemma 4 Good Hackathon
# Kaggle GPU environment requirements (T4 / P100 / L4)

# Core
torch>=2.3.0
transformers>=4.45.0
accelerate>=0.34.0
bitsandbytes>=0.43.0   # 4-bit quantisation

# Tokenizer
sentencepiece>=0.1.99
protobuf>=3.20.0

# Optional: Gradio UI for demo
gradio>=4.40.0

# Dev / local mock validation (no GPU needed)
# python demo_cli.py --mock   → no additional deps required
